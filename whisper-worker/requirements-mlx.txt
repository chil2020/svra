# 宿主機專用（MLX 只跑得動 Apple Silicon，容器裡沒有 Metal）。
#
# 版本釘死的理由：Breeze-ASR-25-mlx-fp16 的作者只驗證過 mlx-audio 0.4.2 與 0.4.5，
# 0.4.6 以後沒測過。ASR 後端出問題的症狀是「安靜地轉出空字串」而不是報錯，
# 所以寧可釘在有人驗過的版本上。
mlx-audio==0.4.5

# 音檔長度用 PyAV 算，跟 faster-whisper 內部用的是同一個函式庫。
# RTF 的分母必須兩邊同一個定義，否則兩個後端的數字不能拿來比。
av>=13,<16
